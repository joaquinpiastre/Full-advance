PORT=3001
DATABASE_URL=postgresql://postgres:password@localhost:5432/fulladvance
JWT_SECRET=cambia_este_secreto_por_uno_seguro
# En Railway, montar un Volume y apuntar UPLOADS_DIR al mount path (ej: /data/uploads)
# para que las fotos no se pierdan en cada redeploy.
UPLOADS_DIR=./uploads

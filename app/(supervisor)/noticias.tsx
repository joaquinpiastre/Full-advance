import NoticiasScreen from '../../components/NoticiasScreen';
import { COLORS } from '../../constants';

export default function SupervisorNoticias() {
  return <NoticiasScreen color={COLORS.supervisor} />;
}

import TareasScreen from '../../components/TareasScreen';
import { COLORS } from '../../constants';

export default function RepartidorTareas() {
  return <TareasScreen color={COLORS.repartidor} />;
}

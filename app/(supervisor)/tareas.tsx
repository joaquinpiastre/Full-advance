import TareasScreen from '../../components/TareasScreen';
import { COLORS } from '../../constants';

export default function SupervisorTareas() {
  return <TareasScreen color={COLORS.supervisor} />;
}

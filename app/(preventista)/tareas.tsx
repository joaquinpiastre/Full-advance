import TareasScreen from '../../components/TareasScreen';
import { COLORS } from '../../constants';

export default function PreventistaTareas() {
  return <TareasScreen color={COLORS.preventista} />;
}
